def add(int a, int b) -> int {
    return a + b;
}

def factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

int result = add(5, 10);
print(result);

int fact = factorial(6);
print(fact);
